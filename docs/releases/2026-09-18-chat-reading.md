# 2026-09-18 问答等待与全文阅读

直接问答曾自动恢复旧报告卡，普通问题又把无关个人历史带入模型；手机上长段落和等待状态都难以判断。本次调整问答上下文、阅读控件和报告展示范围，保留原品牌及两家模型分工。

- 明确一般科普或排除个人资料时，不向模型发送成员趋势、评估、记忆、昵称、Soul及此前个人对话；个人情况和不确定请求继续使用已授权上下文。权限及红线规则优先。
- 默认要求简短回答，明确详细问题允许展开；消息和审计记录本题篇幅偏好及上下文范围。保留完整结束检查，不截断正文，不增加二次压缩调用。
- 等待组件显示真实秒数，10/30秒阶段说明不伪造进度、不自动重发；读屏不每秒打断。
- 无格式长文按完整句分段；长答提供字号调整和回到开头，保持全文、末尾提醒、草稿和主动回看位置。原Markdown结构和安全渲染保持。
- 直接问答不显示旧报告，具体记录只展示相应报告；报告→问答→原报告保留草稿及已收到消息，原服务端缓存/事实校验继续执行。对话显示为“PoopSense 助手”。

## 真实模型检查

隔离虚构演示数据，同一道一般科普问题各请求一次：

| 口径 | 旧版 | 新版 |
| --- | --- | --- |
| 完整API响应 | 11.156秒 | 8.609秒 |
| 汉字数（含参考题名） | 490 | 420 |
| 总字符（含URL/Markdown） | 964 | 1080 |
| 正文段落（不含参考来源） | 6 | 3 |

新版明确详细问题另请求一次，22.906秒、1412汉字/3301总字符，完整返回。样本不足以证明长期提速；100–180字只是提示目标，百川仍超出，参考链接增多也可能使总字符变长。本次不宣称医学准确性或引用支持性已验证。

## 本轮本地验证

- 后端完整425项通过（186.60秒），隔离数据库、阻断真实HTTP及密钥读取。新增29项覆盖上下文排除、个人情况保留、详细追问、完整正文、权限和红线；首次新增fixture把既有API的200误写201导致5项失败，修正测试合同后完整回归通过，失败证据保留。
- 前端完整138项通过；最后将直接问答的误导性“没有已认领记录”改为“直接问答”后，重新TypeScript及构建通过。未将同一全量重复运行。
- 独立Chromium的12组问答交互与补充报告导航检查通过。320/390宽无页面横溢出，10/30秒实际等待与显示相符；千字长文逐字保留，放大字号不重发，模拟300px可见高度和9行草稿下末尾评价可见，迟到回答未抢回看位置。历史来源/危险Markdown/409明确重试保持正确。
- 浏览器业务请求全部mock，不能证明真实模型速度或公网持久保存。初次验收脚本误以为直接问答存在报告卡，选择器空值失败；修正脚本后通过，原证据保留。

## 发布边界

代码`702540702711b68db32878d6f70a4aee91c0a357`已推送main，[GitHub完整检查](https://github.com/koljiu753/poopsense1/actions/runs/35324839005)成功。后端部署`dpl_2L3awf9H7qyDEEjPrpkgLcM6mXL5`与reader前端`dpl_CLCMVDyuuuG6C9Bwe65Q9oUWxWMi`均READY，固定入口返回200，JS `index-6S2fau1O.js` / CSS `index-0CbEecKe.css`与本地构建哈希一致。

16:33–16:34独立公网真实问答两次、无自动重试：DeepSeek返回4.032秒/56汉字；百川返回9.703秒/422汉字（含参考题名，1029总字符）。回执和篇幅/上下文标记正确，百川仍偏长；不同问题的两个时间不能用来排名模型。公网另重复12组mock交互与报告导航检查通过，无捕获JS异常；不以这些mock请求证明线上业务保存。

用户入口保持 https://poopsense-reader-0912.vercel.app/ 。真实微信键盘、长期延迟和演示库跨实例持久化仍未完成验收；平台部署成功不代表正式生产就绪。密钥、供应商分工、worker和硬件能力未在本轮变更。浏览器交互检查使用mock，真实模型检查另行记录，二者不能混同。
