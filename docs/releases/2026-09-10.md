# 2026-09-10 · 记录、回应与导航体验更新

这次更新围绕家庭成员的“找到记录 → 看懂建议 → 留下回应 → 回看变化”流程展开，恢复与海报一致的粉、黄、蓝品牌和角色场景，并补齐跨页操作、浏览器历史与输入面板。

## 主要变化

- 首页用成员日记、日期、角色气泡和近期足迹呈现记录；报告延续同一角色，先显示观察、一项优先建议和回应。已保存选择可在对应记录中找回；紧急和信息不足说明保持完整。
- 健康拆分为记录、行动、趋势。行动页先展示后续可靠观察和主观感受，再提供源报告与可展开依据；采用意向和实际执行保持区分，前后变化不解释为因果。
- 新记录在健康、广场和“我的”通过页面内提醒呈现，不直接打断正在进行的操作。队列优先保留安全项，显示记录时间和待看数量。
- 浏览器返回、前进、刷新及页面内返回使用统一导航。页面地址包含成员、健康分区、趋势范围及报告/行动来源；返回恢复可用的滚动位置和焦点，失效记录给出重新读取或返回入口。
- 同成员切页保留未提交的广场草稿和设置编辑，重新进入时刷新服务端状态；成员或家庭连接范围变化后隔离状态。记录与趋势独立读取，趋势失败不再挡住记录。
- 发布面板支持键盘焦点、关闭后位置恢复，并随浏览器可见区域缩小调整，正文独立滚动；每条发布仍需明确确认。

## 修复

- 旧成员、旧请求和迟到模拟检测结果不再强制打开错误记录；报告按当前成员可查看的记录校验，不用最新记录替换失效链接。
- 报告刷新后仍保留返回关系；重复点击相同页面不重复创建历史条目，同条记录的暂时缺失不重复触发自动分析。
- 保留无记录时的独立咨询入口；保存失败不会显示已保存，迟到读取不会覆盖新选择。
- 公网追问实测发现原报告被清空，现已修复：普通追问等待和完成后保留原报告及已保存回应；运行详情失败也不清空，只有新报告替换报告内容。补充两条回归并通过最终77项检查。
- 8 张常用品牌图新增无损 WebP，合计从 12,256,519 降至 8,681,216 字节，减少 29.2%；逐像素确认可见颜色和透明度一致，原 PNG 保留。地图与设备图按需加载，减少首访传输；不将体积优化解释为网络超时已完全解决。
- 修正用户消息浅黄底继承白字的问题，使用品牌黑色文字，保持同一配色并提高可读性。

## 验证

- 本地前端 **77/77 通过**：App 70、API 3、发布面板 2、模拟器 2；2026-09-10 23:18:07（北京时间）开始，0 失败、0 跳过。TypeScript/Vite 构建通过。
- 本轮后端回归：**103/103 通过（2026-09-10 22:49:21开始，未调用真实模型或硬件）**。
- 本地浏览器检查覆盖 320/390 宽的导航与刷新、90 天范围及成员恢复、报告来源返回、滚动和触发按钮焦点；另有核心页面多尺寸布局检查。
- 发布面板通过 320/390 宽、420/300 可见高度的四组模拟检查。此项模拟的是浏览器可见区域，尚不是手机真机软键盘验收。

## 演示与范围

[打开新版 PoopSense](https://poopsense-family.vercel.app/)。本次发布独立前端，沿用 `poopsense-live-demo.vercel.app` 的现有公网演示后端；浏览器通过同域代理连接，不依赖本地电脑。旧演示后端本轮没有重新部署。

- 2026-09-10 23:04–23:06（北京时间）实际 HTTP 检查：首页、构建 JS/CSS、12 张品牌 PNG 全部 200；图片 SHA-256 与公开资源清单一致。成员、记录、趋势和健康检查正常返回，模拟采集状态为关闭。
- 公网 390px 浏览器成功生成既有虚构记录报告，请求约 44.8 秒；一次不含个人信息的功能介绍问答成功，请求约 6.3 秒。问答后的运行详情接口出现 404，界面保留回答并提示部分跟进信息未加载；这不构成并发或历史持久性保证。
- 最终软件提交 [`873a47d8`](https://github.com/koljiu753/poopsense1/commit/873a47d8ec0e645ee5846caeb50cd40a77774f53) 的 [GitHub Actions](https://github.com/koljiu753/poopsense1/actions/runs/34495502692) 前端测试与构建、后端测试均成功（23:25:58完成）；前一业务提交已核实日志中的前端77项、后端103项。
- 首轮公网广场和“我的”读取曾约20秒超时，同时大PNG缓慢加载；刷新后读取及图片恢复。保留这项失败证据，本次随后加入无损图片压缩，真实慢网体验仍需进一步检验。
- 修复后的公网版本完成报告→追问检查：报告约31.7秒、问答约5.5秒，追问等待/完成后原报告仍在，未再误提示新信号，运行详情200。320px广场和“我的”首次读取正常，角色、地图和设备WebP正常；地图约5.6秒。此前390/1280页面和Sam/90天刷新也已检查。此项不代表真机或所有网络条件。
- 最后的一行文字颜色修正部署到当前最终链接后，23:28确认首页、代码资源、成员记录、health/ready正常；构建资源与本地一致（JS `index-G7JBpR9T.js`、CSS `index-LFgzd53q.css`）。浏览器检查消息样式为黑字 `rgb(11,11,11)` / 浅黄 `rgb(255,242,173)`。业务流程检查使用前一版本，二者仅该文字颜色不同。

公网演示使用共用虚构数据和临时 SQLite，在线模拟采集已禁用，不承诺跨实例或重启持久化。请勿输入真实健康或身份信息。正式独立登录、持久数据库与长期后台任务部署仍未完成。

当前产品面向家庭一般成员，核心为传感记录、可靠卡通、自动报告、生活建议、回应与趋势；机械臂和 VBot 为历史模块，默认关闭。自动化回归没有调用真实模型；公网只完成上述少量实际问答及报告读取，不构成真实硬件全流程、临床有效性或量产验证。
