# 2026-09-21 切回 DeepSeek

按用户要求，问答使用的服务端模型配置已切回 `deepseek-v4-pro`。入口仍为 [PoopSense 问答](https://poopsense-reader-0912.vercel.app/#/chat?member=m_001)。历史百川回答保留原来源，新回答显示本次实际调用来源。

## 配置与发布

- 日常问答、使用帮助、记录解释、周报文字总结、健康知识、紧急情况文字解释六类任务均指向 DeepSeek。
- 自动路由保持开启，单次记录报告与后台动作仍走规则引擎；未改权限、风险规则或模型适配代码。
- 本地服务端与 Vercel 生产/预览配置同步更新，通用接口地址、型号、密钥与任务覆盖一致。密钥未写入前端、文档或 GitHub；本地已有进程未主动重启。
- 生产后端 `dpl_6iQ2GuEzN8wdG1etqNgDJ1dcoAvJ` 为 READY，部署基于 `91387d7`（业务代码仍为 `7025407`）。reader 前端无需重发，通过既有代理访问新后端；旧 family/mobile 入口未重新发布。

## 本轮验证

- 原 reader 首页与静态资源 HTTP 200，JS `index-6S2fau1O.js`、CSS `index-0CbEecKe.css` 与本地最终构建哈希一致。
- 公网状态接口确认六类模型任务全部 DeepSeek、两类任务仍为规则，均已配置。
- 两道虚构测试题只各发送一次，无自动重试。使用帮助题 HTTP 200、3.547 秒、63 字符；健康知识题 HTTP 200、5.500 秒、201 字符。实际回执均为 `deepseek-v4-pro`；后者保持仅本题上下文，二者均为简短模式。
- 本地隔离规则路径回归 1 项通过，确认单次记录报告与后台动作不请求模型；测试阻断真实 HTTP 与本地密钥读取。未将 9/18 的整套测试算作本轮重跑。
- 检查发现旧趋势测试的固定 8/22 样例已移出最近 30 天窗口，定向复现 2 失败/1 通过。只将该测试模块的上传辅助函数改为当前 UTC 前一天，保持 93 秒时长；修复后全模块 8 项通过，没有修改业务逻辑。

两题时延只是当前单次测量，不是模型对比基准或长期速度保证。本轮未验证医学准确性、微信真机键盘或真实硬件上传。公网仍使用共用虚构身份与临时 SQLite，worker 关闭、`production_ready=false`；模型切换没有解决跨实例保存和正式设备登记问题。
